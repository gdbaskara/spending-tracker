# Netlify build config for the Celengin web app (Next.js App Router).
# The official plugin handles SSR/ISR + the App Router automatically.
[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

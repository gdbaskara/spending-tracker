# Supabase — copy to .env.local and fill in. .env.local is gitignored.
# Publishable (anon) key is safe to expose in the browser.
NEXT_PUBLIC_SUPABASE_URL=https://efcnzpnokgushfywaxqt.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxx

# Secret key — server/CLI only. NEVER prefix with NEXT_PUBLIC_, never commit.
# Used only for running migrations/seeds from your machine.
SUPABASE_SECRET_KEY=sb_secret_xxxxxxxxxxxxxxxxxxxx

# Gemini (receipt OCR) — server only. Get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=AIza_xxxxxxxxxxxxxxxxxxxx
